# electro-cart
a fully funtional full stack e-commerce web application.
Uses handlebars templeting engine for templating in frondend.
uses express framework and nodejs for server deployment.
uses mongodb as database management system.
uses razorpay payment integration for payments.
